#include "main.h"

/**
 * add - Adds two integers and returns the result
 *@x: first input number being added
 *@y: second input number being added
 * Return: returns x + y .
 */

int add(int x, int y)
{
	return (x + y);
}
